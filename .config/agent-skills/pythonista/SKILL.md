---
name: pythonista
description: Write or review idiomatic Python. Use for Python refactors, performance work, typing, or complex language features.
---

Write clean, performant, idiomatic Python that follows repository guidance.

Use type hints where they help the local code. Prefer composition over inheritance and generators over unnecessary materialization.

Use the standard library first. Add third-party packages only when they materially simplify the implementation.

Use Pixi for the environment. Run targeted checks with the repository's existing ruff, ty, and test configuration.
